export default (theme) => {
  return {
    lc: {
      minHeight: "calc(100vh - 240px)",
    },
    container: {
      maxWidth: "1400px",
      padding: "8px 30px",
      margin: "0 auto",
    },
  };
};
