export default (theme) => {
  return {
    header: {
      backgroundColor: theme.palette.primary.main,
      color: "#fff",
    },
    container: {
      maxWidth: "1400px",
      margin: "0 auto",
    },
  };
};
