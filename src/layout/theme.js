import { createTheme } from "@mui/material/styles";

export default createTheme({
  typography: {
    fontFamily: "'Poppins', sans-serif",
  },
  palette: {
    primary: {
      main: "#117600",
    },
    background: {
      primary: '#fff',
      shade: "#ececec"
    }
  },
});
